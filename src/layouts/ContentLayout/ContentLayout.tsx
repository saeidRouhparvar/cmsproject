import { Box, Flex } from "@chakra-ui/react";
import {  useState } from "react";

const ContentLayout = ({ prop }) => {

  const [show, setShow] = useState([]);
  
  return (
    <Flex>
 
    </Flex>
  );
};

export default ContentLayout;
